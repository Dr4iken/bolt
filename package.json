{
  "name": "bolt",
  "version": "0.0.10",
  "description": "A simple Signal Library for Typescript and React",
  "main": "dist/bolt.js",
  "types": "dist/bolt.d.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "prepack": "npm run build",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "signals",
    "react signals",
    "useSignal"
  ],
  "author": "Draiken",
  "license": "MIT",
  "publishConfig": {
    "@dr4iken:registry": "https://npm.pkg.github.com"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=10.0.0"
  },
  "dependencies": {
  }
}
